-- ============================================================
-- Migration 013: 채용 페이지 수집 실패 상태 기록
-- 수집 시도가 실패하면 마지막 에러 메시지를 저장해 UI에서 "수집 불가"로 표시한다.
-- 성공하면 NULL로 초기화한다.
-- ============================================================

ALTER TABLE job_sources
  ADD COLUMN IF NOT EXISTS last_scrape_error TEXT;
